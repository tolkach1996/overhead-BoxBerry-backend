const BoxberryModel = require('./boxberry.model');
const CitiesModel = require('./cities.model');
const AuthModel = require('./auth.model');

module.exports = {
    BoxberryModel,
    CitiesModel,
    AuthModel
}